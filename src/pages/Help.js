import React from 'react';
import styled from 'styled-components';

const Help = () => {
  return (
    <Wrapper>
      <h2>Help page.</h2>
    </Wrapper>
  );
};

export default Help;

const Wrapper = styled.section``;
